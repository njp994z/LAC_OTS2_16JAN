import { createSlice } from '@reduxjs/toolkit'
import type { PayloadAction } from '@reduxjs/toolkit'


const layoutManagerSlice = createSlice({
  name: 'layoutManager',
  initialState: {},
  reducers: {
    
  },
})

export const { } = layoutManagerSlice.actions
export default layoutManagerSlice
